treshold = list(filter(lambda element: sum(element.values()) > 4, dominant_vectors))
if len(treshold) > 0:
    print(treshold, len(treshold))
    break

# most epic error finder of all time
for i in mx:
    temp = list(i.values())
    for j in mx:
        temp2 = list(j.values())
        if temp == temp2: continue

        counter = list()
        for k in range(4):
            counter.append(temp[k] > temp2[k])
        c = len(set(counter))
        if c == 1:
            print(temp, temp2)
            print("HEY")